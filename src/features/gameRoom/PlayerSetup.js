import React from 'react';
import {
  selectMyName,
  selectMyTeam,
  selectIsGameActive,
} from '../gameSetup/gameSlice';

export default function PlayerSetup() {

  return (
    <div>
      <h2>
        Player Setup
      </h2>
    </div>
  );
}
